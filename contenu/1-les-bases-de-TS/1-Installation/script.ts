let str: string = "poule";

console.log(str);

// commande terminal --->  tsc script.ts va le compiler
// tsc --init -----> crzation du fichier config
// tsc -w -------> watch les changements --- cela permet d'eviter les compilations à la main

function foo() {
  return "test";
}
